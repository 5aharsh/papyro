# Papyro Configuration

# Path to the content folder (containing site-config.json, posts/ and pages/)
PAPYRO_CONTENT_DIR="./papyro/content"

# Other settings such as title, theme, and nav_order should be managed 
# within the site-config.json file inside your content directory.
